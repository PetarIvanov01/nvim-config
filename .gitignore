tags
test.sh
.luarc.json
nvim

spell/

# nvim-pack-lock.json is deliberately NOT ignored here. Upstream kickstart.nvim
# ignores it to avoid merge conflicts on a template repo; this is a personal
# config, where pinning exact plugin commits across machines is the point.
# See :help vim.pack-lockfile

# Stray runtime logs (nvim.log was committed by accident once).
*.log

.DS_Store
